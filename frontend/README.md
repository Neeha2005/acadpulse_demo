# Frontend

React + Vite web app for AcadPulse.

## Deploy

- Install: `npm install`
- Build: `npm run build`
- Output: `dist/`

## Environment

- Copy `.env.example` to `.env` for local development.
- Set the deployed backend URL through the Vite environment variables used by this app.
